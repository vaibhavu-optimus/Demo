trigger:
- main

pool:
  name: vmss-agent

jobs:
  - job: job1
    steps:
    - script: |
        echo 'Hello 1, $(HOSTNAME)'
  - job: job2
    steps:
    - script: |
        echo 'Hello 2, $(HOSTNAME)'
  - job: job3
    steps:
    - script: |
        echo 'Hello 3, $(HOSTNAME)'
  - job: job4
    steps:
    - script: |
        echo 'Hello 4, $(HOSTNAME)'

